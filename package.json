{
  "name": "rpi-server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build:js": "esbuild entry.js --bundle --outfile=bundle.js",
    "build:css": "postcss styles.css -o output.css",
    "watch:css": "tailwindcss -i styles.css -o output.css --watch",
    "build": "npm run build:css && npm run build:js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "esbuild": "^0.24.0",
    "postcss-cli": "^11.0.0",
    "tailwindcss": "^3.4.14"
  },
  "dependencies": {
    "daisyui": "^4.12.14"
  }
}
